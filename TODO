(x) if directory not found in autoindex -> wrong status code (500 instead of 404)
( ) are the locations correctly choosen? TEST!
(x) CGI file extension must be matched at end of URL
( ) move files to master branch
( ) more tests
( ) big cleanup time
( ) default config file
